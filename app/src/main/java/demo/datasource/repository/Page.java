package demo.datasource.repository;

import java.util.ArrayList;
import java.util.Collection;

/**
 * Created on 23.10.2015.
 */
public class Page<T> extends ArrayList<T> {
	// Page implementation should provide a convenient way to manage the
	// pagination problem. It is expected that it'll provide such properties like so:
	// - current page position;
	// - items count;
	// - items enumeration;
	// ? recursive data window movement within the page.
}
